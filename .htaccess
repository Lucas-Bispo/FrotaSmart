Options -Indexes

<FilesMatch "^(\.env|composer\.(json|lock)|AGENTS\.md)$">
    Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(backend|src|scripts|vendor|AI|docs)/ - [F,L]
</IfModule>
